``ExePathRef.can_run`` now checks the group and other execute bits instead of only the owner one, and returns ``False``
rather than ``None`` when a file carries none of the three - by :user:`darrenhuai`.
