Run the flaky GraalPy CI job's tests without ``pytest-xdist``, since ``execnet``'s controller thread was still alive
when GraalVM closed its context, crashing the job even after every test passed, and add crash-recovery reruns plus a
longer per-test timeout for the runs that remain.
