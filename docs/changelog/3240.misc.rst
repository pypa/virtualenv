Run the flaky GraalPy CI job's worker pool at ``-n 2`` instead of ``-n auto`` to cut the JIT warmup contention that was
pushing tests past their timeout, add crash-recovery reruns, and raise its per-test timeout to give slower runs enough
room.
