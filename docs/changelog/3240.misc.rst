Fix flaky GraalPy CI by dropping ``pytest-xdist`` and its ``--timeout-method=thread`` override for that job. The watcher
thread that method spawns per test was still alive when GraalVM closed its interpreter context, crashing the job even
after every test passed. Add crash-recovery reruns plus a longer per-test timeout for the runs that remain.
