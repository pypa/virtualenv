Recreating an environment whose interpreter has since been removed no longer writes through the stale symlink left
behind. ``ensure_safe_to_do`` cleared the destination based on ``exists()``, which follows the link, so ``--symlinks``
failed with a ``FileExistsError`` and ``--copies`` reported success while placing the interpreter outside the
environment - by :user:`darrenhuai`.
