Stop the bash activator from re-expanding the prompt inside ``PS1``, so a virtual environment name or ``--prompt`` value
containing ``$(...)``, backticks or backslashes no longer runs as a command each time bash draws the prompt - by
:user:`gaborbernat`.
