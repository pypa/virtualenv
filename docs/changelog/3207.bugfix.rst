``ListType.split_values`` now returns a ``list`` for ``str`` input rather than a one-shot ``filter`` object, and decodes
``bytes`` input so that it splits on commas and yields ``str`` values - by :user:`darrenhuai`.
